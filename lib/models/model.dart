enum Type { circle, square, cross }

class FibonanciModel {
  final int id;
  final int number;
  final Type type;

  FibonanciModel(
      {required this.id, required this.number, required this.type});

}
